import { FilesValidationPipe } from './files.validation.pipe';

describe('FilesValidationPipe', () => {
  it('should be defined', () => {
    expect(new FilesValidationPipe()).toBeDefined();
  });
});
