import { IdentificationGuard } from './identification.guard';

describe('IdentificationGuard', () => {
  it('should be defined', () => {
    expect(new IdentificationGuard()).toBeDefined();
  });
});
